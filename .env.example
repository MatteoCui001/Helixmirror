# Helix Mirror 环境变量配置
#
# 复制此文件为 .env.local 并填入实际值

# ===========================================
# API 认证（生产环境必需）
# ===========================================

# API Token，用于保护 API 端点
# 生成方式：openssl rand -hex 32
# 请求时在 Header 中添加：Authorization: Bearer YOUR_TOKEN
API_TOKEN=your-secret-token-here

# ===========================================
# 数据库配置
# ===========================================

# SQLite 数据库路径（可选，默认 ./data/helix.db）
# DATABASE_PATH=./data/helix.db

# ===========================================
# Next.js 配置
# ===========================================

# 开发/生产环境
NODE_ENV=development

# 应用基础 URL
# NEXT_PUBLIC_APP_URL=http://localhost:3000
