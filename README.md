# Helix Mirror 🧬

统一的多 Agent 管理与协同系统 —— 观察、理解、优化你的 AI 助手网络。

![Dashboard Preview](https://via.placeholder.com/800x400/1e1b4b/6366f1?text=Helix+Mirror+Dashboard)

## 快速开始

### 1. 安装依赖

```bash
cd ~/Projects/helix-mirror
npm install
```

### 2. 初始化数据库

```bash
npm run db:init
```

### 3. 启动开发服务器

```bash
npm run dev
```

### 4. 打开浏览器

访问 http://localhost:3000

---

## 项目结构

```
helix-mirror/
├── SPEC.md                 # 产品需求文档
├── README.md               # 本文件
├── package.json            # 项目配置
├── next.config.js          # Next.js 配置
├── tailwind.config.js      # Tailwind CSS 配置
├── tsconfig.json           # TypeScript 配置
├── scripts/
│   └── init-db.js          # 数据库初始化脚本
├── src/
│   ├── app/
│   │   ├── layout.tsx      # 根布局
│   │   ├── page.tsx        # 主仪表盘页面
│   │   └── globals.css     # 全局样式
│   ├── components/
│   │   ├── AgentCard.tsx   # Agent 统计卡片
│   │   ├── StatCard.tsx    # 概览统计卡片
│   │   ├── ActivityList.tsx # 最近活动列表
│   │   └── AgentActivityChart.tsx # 活跃度图表
│   └── lib/
│       ├── db.ts           # 数据库连接
│       └── queries.ts      # 数据查询
└── data/
    └── helix.db            # SQLite 数据库文件
```

---

## 核心功能

### MVP（已实现）

- ✅ **透明代理层** - 统一记录所有 Agent 交互
- ✅ **Web 仪表盘** - 可视化展示今日概览、Agent 统计、最近活动
- ✅ **本地数据库** - SQLite 存储，无需外部服务
- ✅ **图表可视化** - 用柱状图对比各 Agent 活跃度

### 后续演进

- 🔄 **智能路由** - 基于意图自动选择 Agent
- 🔄 **记忆共享层** - 项目信息跨 Agent 共享
- 🔄 **云端部署** - 支持远程访问
- 🔄 **进化看板** - Agent 能力提升可视化

---

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端框架 | Next.js 15 + React 19 |
| 类型系统 | TypeScript |
| 样式方案 | Tailwind CSS |
| 图表库 | Recharts |
| 数据库 | SQLite (better-sqlite3) |
| 部署 | 本地 / Vercel（可选） |

---

## 数据同步（手动）

目前数据需要手动同步。你可以在脚本中读取 OpenClaw 的 memory 文件，然后调用数据层 API：

```typescript
import { addInteraction } from '@/lib/queries';

// 添加一条交互记录
addInteraction('craft', 'Discord', '讨论 Helix Mirror 架构', 5);
```

后续可以开发自动同步脚本。

---

## 设计原则

1. **渐进式演进** - 从本地 MVP 开始，逐步上云
2. **单点登录** - 所有 Agent 信息汇总到统一仪表盘
3. **视觉优先** - 用图表和颜色快速传递信息
4. **代码即文档** - 详细的代码注释，方便维护

---

## License

MIT
